rootProject.name = "spring-angular2-component-based"

new File(rootDir, "applications").eachDir { dir -> include "applications/${dir.name}" }
new File(rootDir, "components").eachDir { dir -> include "components/${dir.name}" }

include "feature-tests"